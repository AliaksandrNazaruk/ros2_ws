# AEHub stack - environment file (example)
#
# Copy to /etc/aehub/aehub_stack.env and edit values.
# Keep secrets OUT of git.
#

# ROS / DDS
ROS_DOMAIN_ID=0
RMW_IMPLEMENTATION=rmw_fastrtps_cpp

# AEHub / config service
ROBOT_ID=fahrdummy-01-local
CONFIG_SERVICE_URL=http://localhost:7900
CONFIG_SERVICE_API_KEY=tR-UZ2j2KutE6OYlEGbsx0h5qe071L-gC5kd1hHKfw4

# Launch flags (must be 'true'/'false' strings for launch substitutions)
USE_SIM_TIME=false
AUTOSTART=true
TLS_VERIFY=true
SYMOVO_AUTO_ENABLE_DRIVE_MODE=true

# Optional: Symovo endpoint override (if your launch supports it via args)
# SYMOVO_ENDPOINT=https://192.168.1.100
# AMR_ID=15
