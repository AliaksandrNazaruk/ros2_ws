# Runbook (Canonical)

This repository uses a single canonical runbook:

- `SPECIFICATION.md` section "7A. Runbook / Operational checks (mandatory)"

Do not duplicate or override procedures here. This file exists only to point to the
canonical source of truth.
